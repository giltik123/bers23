name: Fashion F4b.6d.9c manual body-anchor editor

on:
  pull_request:
    paths:
      - 'src/components/editor/outfits/CanonicalTryOnBodyAnchorEditor.jsx'
      - 'src/application/fashion/canonicalTryOnManualBodyAnchorDraft.js'
      - 'src/application/fashion/canonicalTryOnManualAcquisition.js'
      - 'src/application/fashion/createCanonicalTryOnManualPrerequisiteApplication.js'
      - 'tests/fashion-tryon-manual-body-anchor-draft.test.mjs'
      - '.github/workflows/fashion-tryon-manual-body-anchor-editor-f4b6d9c.yml'

permissions:
  contents: read

jobs:
  manual-body-anchor-editor:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0
      - name: Assert exact PR head SHA
        run: test "$(git rev-parse HEAD)" = "${{ github.event.pull_request.head.sha }}"
      - uses: actions/setup-node@v4
        with:
          node-version: 24.x
          cache: npm
      - name: Install exact frontend graph
        run: npm ci
      - name: Prove body-anchor draft semantics and browser boundary
        run: node --test tests/fashion-tryon-manual-body-anchor-draft.test.mjs
      - name: Typecheck and build production frontend
        run: |
          npm run typecheck
          npm run build
      - name: Prove explicit-point-only UI and retained Try-On lockdown
        run: |
          set -euo pipefail
          COMPONENT=src/components/editor/outfits/CanonicalTryOnBodyAnchorEditor.jsx
          HELPER=src/application/fashion/canonicalTryOnManualBodyAnchorDraft.js
          grep -q 'normalizeManualBodyAnchorEditorSource(source)' "$COMPONENT"
          grep -q 'validateManualBodyAnchorDraft(safeSource?.category, anchors)' "$COMPONENT"
          grep -q 'Core remains the final destination-geometry' "$HELPER"
          grep -q 'PROJECT_IMAGE_NORMALIZED' src/application/fashion/canonicalTryOnManualAcquisition.js
          ! grep -E 'coreClient|fetch\(|representationId|anchorSetId|storageId|contentSha256|ticketId|executionId|destinationMesh|FASHN|Billing|credits|outfitManager|tryonEngine' "$COMPONENT"
          grep -q 'TRYON_EXECUTION_NOT_WIRED' src/lib/tryon/tryonEngine.js
      - name: Check candidate diff whitespace
        run: git diff --check "${{ github.event.pull_request.base.sha }}..HEAD"
