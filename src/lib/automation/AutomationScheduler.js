export const automationScheduler = { nextRun(automation, from = new Date()) { if (!automation.schedule || automation.schedule === 'manual_only' || automation.schedule === 'immediate') return null; const next = new Date(from); if (automation.schedule === 'daily') next.setDate(next.getDate() + 1); if (automation.schedule === 'weekly') next.setDate(next.getDate() + 7); return next.toISOString(); }, canRun(automation) { return automation.schedule !== 'manual_only' || automation.trigger === 'manual_execution'; } };