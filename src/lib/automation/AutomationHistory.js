import { coreClient } from '@/api/coreClient';
export const automationHistory = { list() { return coreClient.entities.AutomationRun.list('-executed_at', 100); }, record(data) { return coreClient.entities.AutomationRun.create({ ...data, executed_at: new Date().toISOString() }); } };