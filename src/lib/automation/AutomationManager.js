import { automationLibrary } from '@/lib/automation/AutomationLibrary';
export const automationManager = { list() { return automationLibrary.list(); }, create(data) { return automationLibrary.create({ ...data, automation_id: data.automation_id || `automation-${Date.now()}`, created_at: new Date().toISOString(), updated_at: new Date().toISOString() }); }, update(automation, patch) { return automationLibrary.update(automation.id, { ...patch, updated_at: new Date().toISOString() }); }, favorite(automation) { return this.update(automation, { favorite: !automation.favorite }); } };