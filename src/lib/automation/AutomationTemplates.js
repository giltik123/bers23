const action = (type, config = {}) => ({ id: `${type}-${Math.random().toString(36).slice(2, 7)}`, type, ...config });
export const AUTOMATION_TEMPLATES = [
  ['Marketplace Product', 'Store', [action('replace_background'), action('enhance_quality'), action('export')]], ['Instagram Post', 'Instagram', [action('enhance_quality'), action('generate_variants', { count: 3 }), action('export')]], ['Portrait Enhancement', 'UserRound', [action('enhance_quality'), action('upscale'), action('create_version')]], ['Luxury Product', 'Gem', [action('replace_background'), action('enhance_quality'), action('generate_variants', { count: 2 })]], ['Fashion Shoot', 'Shirt', [action('run_try_on'), action('run_creative_strategy'), action('export')]], ['Real Estate', 'House', [action('enhance_quality'), action('replace_background'), action('export')]], ['Vehicle Photography', 'Car', [action('enhance_quality'), action('generate_variants', { count: 2 })]], ['Food Photography', 'Utensils', [action('enhance_quality'), action('run_recipe')]], ['Batch Background Removal', 'Layers', [action('replace_background'), action('export')]],
].map(([name, icon, actions], index) => ({ automation_id: `template-${index + 1}`, name, description: `${name} workflow`, icon, trigger: index === 8 ? 'batch_execution' : 'manual_execution', conditions: [], actions, variables: [], estimated_credits: actions.length * 30, estimated_time: actions.length * 30000, schedule: 'manual_only', metadata: { template: true } }));