import React from 'react';
import { Heart, Plus } from 'lucide-react';
import { Button } from '@/components/ui/button';
import { useTranslation } from '@/lib/i18n/TranslationProvider';
export default function AutomationLibraryPanel({ automations, selectedId, onSelect, onCreate, onFavorite }) { const { t } = useTranslation(); return <aside className="rounded-2xl border border-border/60 p-3"><div className="mb-3 flex items-center justify-between"><p className="text-sm font-medium">{t('automation.library')}</p><Button size="icon" variant="ghost" onClick={onCreate}><Plus className="h-4 w-4" /></Button></div><div className="space-y-1">{automations.length ? automations.map((automation) => <button key={automation.id} onClick={() => onSelect(automation)} className={`flex w-full items-center justify-between rounded-lg px-2 py-2 text-left ${selectedId === automation.id ? 'bg-primary text-primary-foreground' : 'hover:bg-accent'}`}><span><span className="block text-sm font-medium">{automation.name}</span><span className="block text-[10px] capitalize opacity-70">{automation.trigger.replaceAll('_', ' ')}</span></span><Heart onClick={(event) => { event.stopPropagation(); onFavorite(automation); }} className={`h-3.5 w-3.5 ${automation.favorite ? 'fill-current' : ''}`} /></button>) : <p className="text-xs text-muted-foreground">{t('automation.emptyLibrary')}</p>}</div></aside>; }