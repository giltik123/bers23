export interface AgentRequest { readonly request: string; readonly tenantId: string; readonly userId: string; readonly projectId?: string; readonly budget?: Readonly<{ readonly plan: 'free' | 'pro' | 'studio'; readonly credits: number }>; readonly metadata?: Readonly<Record<string, unknown>>; }
export interface AgentContext { readonly tenantId: string; readonly userId: string; readonly projectId?: string; readonly memory: readonly unknown[]; readonly intelligence: Readonly<Record<string, unknown>>; readonly budget?: AgentRequest['budget']; readonly metadata: Readonly<Record<string, unknown>>; }
export function freezeAgentContext(context: AgentContext): AgentContext { return Object.freeze({ ...context, memory: Object.freeze([...context.memory]), intelligence: Object.freeze({ ...context.intelligence }), metadata: Object.freeze({ ...context.metadata }) }); }
