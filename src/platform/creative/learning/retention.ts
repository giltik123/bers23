import { immutableCopy } from './immutable'; import type { CreativeLearningRecord } from './types';
export interface RetentionPolicy { version:string; action:'ANONYMIZE'|'AGGREGATE'|'EXPIRE'; afterDays:number }
export function applyRetention(record:CreativeLearningRecord,policy:RetentionPolicy,now=new Date()):CreativeLearningRecord|null { const elapsed=(now.getTime()-Date.parse(record.occurredAt))/86400000;if(elapsed<policy.afterDays)return record;if(policy.action==='EXPIRE')return null;if(policy.action==='AGGREGATE')return immutableCopy({...record,scope:{tenantId:record.scope.tenantId,projectId:'aggregate',userId:'aggregate'},context:{},goal:{},candidateSet:[],selectedCandidate:{candidateId:'aggregate'},deviceContext:{class:record.deviceContext.class}}) as CreativeLearningRecord;return immutableCopy({...record,scope:{...record.scope,userId:'anonymous'},context:{},goal:{}}) as CreativeLearningRecord; }
